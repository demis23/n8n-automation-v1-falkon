module.exports = {
    // Этот файл содержит логику генерации текста и промпты.
    // Вы можете менять эти шаблоны без изменения основной логики n8n.

    // Шаблон для генерации текста по фото
    textGenerationPrompt: (description, length = 'short') => {
        return `Вот описание картинки: "${description}". 
Напиши креативный и привлекающий внимание пост для Instagram и Facebook.
Тон: дружелюбный, вовлекающий.
Хэштеги: добавь 3-5 релевантных хэштега.
Длина: ${length === 'short' ? 'около 3 предложений' : 'около 2 абзацев'}.`;
    },

    // Шаблон для генерации картинки по тексту
    imageGenerationPrompt: (text) => {
        const prompt = `Создай высококачественное фотореалистичное изображение для поста в соцсетях на основе этого текста: "${text}".
Стиль: современный, яркий, профессиональная фотография.`;
        return prompt;
    },

    // Функция для форматирования финального сообщения
    formatPost: (text, platform) => {
        // Можно добавить специфичные для платформы вещи
        if (platform === 'instagram') {
            return `${text}\n\n👉 Ссылка в профиле`;
        }
        return text;
    }
};
